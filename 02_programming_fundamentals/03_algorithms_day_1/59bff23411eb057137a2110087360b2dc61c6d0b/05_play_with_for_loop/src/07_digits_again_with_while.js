// ## Build an array
//
// Create the famous `digits` array using a `while-loop`.

const digits = [];
let stout = digits.length;

while(stout < 10){
  digits.push(stout);
  stout++;
  //console.log(digits);
}